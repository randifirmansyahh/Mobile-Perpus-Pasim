import React from 'react';
import {View} from 'react-native';
import n from 'react-native-normalize';

const GarisKecil = () => {
  return (
    <View
      style={{
        marginTop: n(4),
        backgroundColor: '#f2f2f4',
        height: n(4),
        marginHorizontal: n(16),
      }}
    />
  );
};

export default GarisKecil;
